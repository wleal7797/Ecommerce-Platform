spring.application.name=Ecommerce Platform

# ?? Conexión PostgreSQL ???????????????????????????????????
spring.datasource.url=jdbc:postgresql://localhost:5432/ADONAI_ECOMMERCE
spring.datasource.username=postgres
spring.datasource.password=Proverbios22:6_EL_4DM1N
spring.datasource.driver-class-name=org.postgresql.Driver

# ?? JPA / Hibernate ???????????????????????????????????????
spring.jpa.show-sql=true
# none = no tocar las tablas
spring.jpa.hibernate.ddl-auto=none
# Le dice a Hibernate que use el schema "ecommerce"
spring.jpa.properties.hibernate.default_schema=ecommerce
# Quitar el warning del dialecto (Hibernate lo detecta solo)
# spring.jpa.properties.hibernate.dialect=  <-- eliminar esta línea
spring.jpa.open-in-view=false

# ?? Thymeleaf ?????????????????????????????????????????????
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.cache=false

# ?? Spring Security: desactivado temporalmente ???????????
# Con SecurityConfig.java en el proyecto esto ya no hace falta,
# pero lo dejamos por si acaso para desarrollo local
spring.security.user.name=admin
spring.security.user.password=admin123
